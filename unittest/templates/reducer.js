/* global describe, it */
import expect from 'expect';
import expectImmutable from 'expect-immutable';

import * as types from '../../constants';

expect.extend(expectImmutable);

const initialState = null;

describe('<%= suiteDesc %>', () => {
    it('will have more than one test case, I promise', () => {
        //
    });
});
