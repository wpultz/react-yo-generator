/* global describe it */
import React from 'react';
import { shallow } from 'enzyme';
import expect from 'expect';

describe('<%= suiteDesc %>', () => {
    it('will have more than one test, I promise', () => {
        //
    });
});
